Alert notifications — Slack, email, Teams webhooks when alerts fire. Essential for a SOC — no one watches a dashboard 24/7.

Dashboard real endpoints — the KPIs (risk trend, MTTD, alert spark) are still hardcoded placeholders. Wire them to real API aggregation queries.

RBAC in the UI — hide pages/actions based on user roles (admin sees everything, analyst sees alerts/cases/search, viewer is read-only).

Reporting — scheduled PDF/CSV exports (incident summary, LGPD compliance, weekly SOC report).

Log retention policies — ClickHouse TTL for hot/warm/cold storage tiers.

Alert enrichment — auto-enrich alerts with VirusTotal, AbuseIPDB, Shodan lookups.