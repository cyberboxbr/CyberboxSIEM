title: High Volume Firewall Pass from Single External IP
id: 0907f5d6-c2a5-4e3a-bf72-6f2000000003
status: experimental
description: |
  Disabled — requires external IP extraction from filterlog CSV which
  the current Sigma compiler cannot do. Re-enable when the enrichment
  pipeline extracts src_ip/dst_ip as top-level fields.
logsource:
  product: linux
  service: syslog
detection:
  selection:
    app_name: __disabled_placeholder__
  condition: selection
level: high
tags:
  - attack.initial_access
  - attack.t1190
falsepositives:
  - N/A (rule disabled)
