title: Outbound Connections to Multiple External IPs
id: 0907f5d6-c2a5-4e3a-bf72-6f2000000002
status: experimental
description: |
  Disabled — firewall filterlog pass,out events include all normal NAT
  traffic (DNS, HTTP, etc.) which generates thousands of events per
  minute from internal hosts. Re-enable when the collector extracts
  actual destination IPs as top-level fields for proper filtering.
logsource:
  product: linux
  service: syslog
detection:
  selection:
    app_name: __disabled_placeholder__
  condition: selection
level: medium
tags:
  - attack.command_and_control
  - attack.t1071
falsepositives:
  - N/A (rule disabled)
